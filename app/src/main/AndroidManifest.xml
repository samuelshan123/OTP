<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.edu.firebasephoneotp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".travelwithin"></activity>
        <activity android:name=".internationalflightreg" />
        <activity android:name=".DomesticFlightreg" />
        <activity android:name=".Trainreg" />
        <activity android:name=".comminginside" />
        <activity android:name=".OutsideChennai" />
        <activity android:name=".withingreaterchennai" />
        <activity android:name=".Commercilalestablishment" />
        <activity android:name=".travelviareg" />
        <activity android:name=".Profile" />
        <activity android:name=".VerifyPhoneActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>